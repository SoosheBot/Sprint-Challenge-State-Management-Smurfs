const initialState = {
    smurfs: [],
    isFetching: false,
    isPosting: false,
    error: ''
};

const rootReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};

